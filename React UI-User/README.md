
  # Figma Prototype for SecureInsure

  This is a code bundle for Figma Prototype for SecureInsure. The original project is available at https://www.figma.com/design/t3xtvNCsRpZcQ5RNJAwkcF/Figma-Prototype-for-SecureInsure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  